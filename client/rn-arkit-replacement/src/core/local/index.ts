/**
 * Device-local concerns (cache, preferences, small offline payloads).
 * Reserved for future use; keeps the core/local slice from the architecture spec.
 */
export const LOCAL_SCHEMA_VERSION = 1 as const;
